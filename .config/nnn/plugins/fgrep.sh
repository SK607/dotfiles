#!/usr/bin/env sh

printf 'TESTESTTEST'

#. "$(dirname "$0")"/.nnn-plugin-helper
#
#printf "pattern: "
#read -r pattern

#if [ -n "$pattern" ]; then
#    printf "%s" "+l" > "$NNN_PIPE"
#    eval "grep -r $pattern" > "$NNN_PIPE"
#fi
